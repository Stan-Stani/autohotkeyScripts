; When hotkeys are pressed to start gif capture with ShareX we want to also toggle clickhighlighting on via below command
SendInput("#+H")
; Then we need some way to trigger an action only in this case that toggles the click highlighting off.